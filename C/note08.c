/*표준 스트림과 일반 스트림에 대한 설명.*/

char buf[100];    ///문자열 이름을 buf라고 써 놓으니까 버퍼인줄알고 헷갈렸다.

gets(buf); //이 문장은 fgets(stdin, buf); 와 같다.

puts(buf); //이 문장은 fputs(stdout, buf); 와 fputs(stderr, buf); 와 같다.

//printf, scanf도 fprintf, 





*/
